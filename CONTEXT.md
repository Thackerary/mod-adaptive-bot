# mod-adaptive-bot 项目核心架构与开发上下文记忆

## 1. 核心架构模式：分层自适应系统 (Hierarchical Adaptive System)

本项目基于 AzerothCore (WoW 3.3.5a) 构建轻量化、不侵入核心源码的随从 AI，采用三层解耦闭环架构：

```text
[ 顶层：持久化机制记忆库 (SQLite) ]
  - 存储索引：(Boss_Entry_ID, Bot_GUID / Spec_ID)
  - 动态下发：Boss 技能轴、高危禁区坐标、打断压秒时机、玩家坦克 TPS 仇恨延后阈值
         ▲
         │ 异步反哺 / 参数覆盖
[ 中层：战斗回溯与归因分析器 (Post-Combat Attribution) ]
  - 触发时机：仅在团灭脱战 (EnterEvadeMode) 或 Boss 击杀 (JustDied) 时异步执行，零战斗运行时 CPU 开销
  - 分析维度：承伤与致死 SpellID、漏打断统计、起手 OT 归因、主坦前 10 秒 TPS 速率测定
         ▲
         │ 运行时数据沉淀
[ 底层：专家动作优先级列表 (APL Baseline) ]
  - 运行实体：轻量级 CreatureAI，基于 OnLevelSynced 动态属性投影与 ApplyPassiveTalents 被动补偿
  - 核心功能：提供开箱即用 70 分 baseline，执行由顶层动态微调的实战战术

```

### 顶层进化四维定义

* **维度 A（打断演进）：** 从“盲目秒断”演化为“识别 Boss 灭团级致命技能，并在读条达到 80% 时压秒打断”，最大化输出偷跑窗口。


* **维度 B（走位自适应）：** 归因器提取高危致死技能来源坐标，将其标记为不可停留禁区，随从在 Boss 施法前摇阶段提前执行侧移规避。


* **维度 C（仇恨协同）：** 依据玩家主坦建立仇恨的前 10 秒 TPS，动态延后输出随从的爆发技能与主力饰品激活时间（0~10s），根除起手 OT 猝死。


* **维度 D（阶段对齐）：** 记忆 Boss 阶段转换阈值（如 20% 狂暴/斩杀期、易伤虚弱期），在机制最关键窗口集中交出嗜血/英勇与核心爆发底牌。



---

## 2. 基类约定与核心可用 API (AdaptiveBotAI.h)

所有随从专精必须继承自统一基类 `AdaptiveBotAI`，且必须优先复用以下基类契约与接口：

### 虚函数重写规范

* `bool IsHealerBot() const override`：治疗专精返回 `true`，输出/坦克专精返回 `false`。


* `bool IsRangedBot() const override`：远程与治疗返回 `true`，接管远程站位；物理近战返回 `false`。


* `bool IsRangedPhysicalBot() const override`：猎人重写为 `true`，与法系远程的法伤加成彻底解耦。


* `float GetDamageDealtMultiplier() const override`：治疗专精压低输出返回 `0.25f`；纯输出与坦克专精固定返回 `1.0f`（严禁误继承法系随从的 2.0x~3.3x 装备倍率通道）。


* `uint8 GetTalentSpellMinLevel(uint32 spellId) const override`：以 `switch-case` 显式注册天赋与特权技能的最低解锁等级。


* `void Reset() override`：配置能量池类型（法力/怒气/能量），清空专精自管计时器与状态机标记，调用基类 `Reset()` 并执行 `ApplyPassiveTalents()`。


* `void OnLevelSynced(uint8 level) override`：同步等级与对应资源池上限，调用 `ApplyPassiveTalents()` 注入被动光环补偿。



### 核心基类可用接口

* **法术判定与执行：**
* `uint32 GetAppropriateRank(uint32 spellId, bool isTalent = false) const;`（在 `GetTalentSpellMinLevel` 声明的技能必须传 `true`）


* `bool CanCast(Unit* target, uint32 spellId, bool checkRangeAndLOS = true) const;`

* `bool ExecuteSpell(Unit* target, uint32 spellId, bool checkRangeAndLOS = true);`



* **目标定位与仇恨：**
* `Unit* SelectAssistTarget();`（获取协助目标）


* `Unit* GetGroupTank();`（获取团队主坦）


* `Player* GetMaster();`（获取随从指挥官）


* `me->Attack(victim, false);`（远程与治疗专精仅锚定目标供法术施放，第二参数传 `false` 绝不开启近战追击）


* `me->Attack(victim, true);`（物理近战必须传 `true` 开启底层自动白字挥砍与双持平砍循环）




* **移动与控制：**
* `void UpdateFollowMaster(uint32 diff);`（脱战通用跟随）


* `me->GetMotionMaster()->MoveFollow(target, dist, angle);`

* `me->StopMoving();`（仅在决定施放非瞬发读条法术前夕按需调用，瞬发与移动施法严禁调用）





---

## 3. 必须严格遵守的底层引擎铁律 (Avoidance Guide)

1. **全局施法与通道双保险守卫（Casting & Channel Guard）：** 治疗与法系 `UpdateAI()` 顶部必须执行 `if (me->HasUnitState(UNIT_STATE_CASTING) || me->GetCurrentSpell(CURRENT_CHANNELED_SPELL)) return;`，杜绝长读条或引导类法术（如苦修、希望圣歌、奥术飞弹、唤醒、吸取灵魂、精神鞭笞、消散）被跟随移动指令打断。


2. **MoveFollow 相对弧度机制：** `MoveFollow(target, dist, angle)` 中的 `angle` 原生代表相对目标当前朝向的弧度偏移（引擎内部自动叠加目标朝向）。**严禁写成 `target->GetOrientation() + angle**`。正后方背身位恒传 `static_cast<float>(M_PI)`，贴坦避难传背身侧向，严禁传 `0.0f`（防止冲到 Boss 脸前吃顺劈与吐息暴毙）。


3. **天赋契约参数闭环：** 3.3.5a 中所有天赋法术在 DBC 中的 `SpellLevel` 均为 0。调用 `GetAppropriateRank(id, isTalent)` 时，所有在 `GetTalentSpellMinLevel` 注册的技能第二参数必须显式为 `true`；基础法术（如暗影魔、希望圣歌、基础图腾、滋养、生命之绽、稳固射击、奥术冲击、复仇之怒、圣洁护盾、旋风斩、斩杀、腐蚀术、吸取灵魂、愤怒、星火术、月火术、激活、树皮术、压制、撕裂、猛击、英勇打击、伏击、背刺、刺骨、割裂、切割）传 `false`。


4. **驱散正向光环过滤与类型精准解耦：**
* 队友自身的增益在底层属性中多为可驱散类型，驱散时必须校验 `if (application->IsPositive()) continue;`，防止无限驱散自己人直至空蓝。


* 驱散通道必须解耦独立判定，严禁使用级联 `if (!dispel)` 导致高优先级驱散就绪后彻底短路其他驱散类型。


* 各职业驱散范围必须硬性收敛：萨满仅解诅咒/疾病/中毒（杜绝魔法）；德鲁伊仅解诅咒与中毒（`DISPEL_CURSE | DISPEL_POISON`，杜绝魔法与疾病）。




5. **宠物特权与血线彻底剥离：**
* 打地鼠雷达的 `lowestHpAlly`、`lowestHpPct` 与 `averageHpPct` 严禁统计宠物（`unit->ToPet() != nullptr`），防止宠物掉血引发全队恐慌。


* 守护之魂、痛苦压制、圣疗术、真言术：盾、大地之盾、迅捷治愈、生命之绽等核心救命大招与主力 HoT 严禁对宠物施放。




6. **战时急救门禁覆盖（防治疗倒挂）：** 辅助增益、全团预铺盾、长读条群抬或图腾矩阵展开前，必须检查团队最低血线，全队出现重伤（`< 60%~70%`）时施法权必须无条件让渡给单体急救。


7. **瞬发跑位施法与 StopMoving 解耦：** `me->StopMoving()` 必须精准下沉至确认施放非瞬发读条法术前夕；瞬发技能（盾、震击、激流、回春、瞬发奥冲、奥术弹幕、毒伤、十字军打击、神圣风暴、嗜血、瞬发猛击、痛苦诅咒、瞬发腐蚀术、月火术、虫群、台风、暗言术：痛、噬灵瘟疫、暗言术：灭、出血、预谋、刺骨、割裂、暗影步等）允许在跑位或避难途中直接施放，保障机动性。


8. **Off-GCD 技能防空转与当帧顺下：** 对不占公共冷却的瞬发法术（如心灵专注、潮汐之力、自然迅捷、急速射击、冷血、暗影斗篷、奥术强化、法术反制、复仇之怒、死亡之愿、鲁莽、血性狂暴、狂暴之怒、树皮术、暗影之舞、伺机待发），施放成功后**严禁 `return true**`，必须允许当帧决策流顺下执行，使增益光环能当帧立即被后续主力技能消费。


9. **光环防顶守卫：** 弹跳类或唯一类光环（愈合祷言），必须全团扫描光环存在性，避免重复施放白白顶掉层数或浪费 GCD。


10. **叠层 Buff 与充能 Buff 检测解耦（Stacks vs Charges）：** 叠加型光环（好运、潮汐奔涌、生命之绽三花、奥术冲击）必须使用 `GetStackAmount()` 检测；充能扣减型光环（大地之盾）必须通过 `aura->GetCharges() > 2` 校验剩余次数，杜绝无脑刷新空蓝。


11. **元素护盾排己守卫（Dual Shield Conflict）：** 萨满同目标只能存在一种元素护盾。主坦锚点退化时（如单人无坦），`MaintainEarthShield` 必须显式排除 `tank == me`，随从自身恒定维持水之护盾回蓝，大地之盾仅对他人主坦维持，杜绝双盾互顶死循环。


12. **瞬发战略大招防吞保护：** 自然迅捷等爆发瞬发增益，激活后若濒死目标脱离阈值，必须降级给全队最低血线成员立即当帧打出瞬发大加，绝不允许光环遗留至后续低级填充技造成浪费。


13. **形态锁定与施法限制破除（Shapeshift Exemption & Instant Shifting）：** 3.3.5a 树形态硬性禁止施放平衡系技能，暗影形态硬性禁止施放圣光系法术。随从脱战补爪子/坚韧/心火/暗抗或驱除诅咒时，必须瞬拔形态（无 GCD），增益施加完毕后自动切回；战时严禁拔形态，增益无条件让路给输出。


14. **主坦双 HoT 优先级防饥饿调度（EDF Scheduling）：** 奶德维护主坦 HoT 时，回春术缺失时必须前置优先补齐；仅在三花极度濒危（`GetDuration() < 2000ms`）时才允许抢先续订，放宽三花补刷窗口至 4000ms，杜绝回春永久饿死。


15. **愈合直接治疗兜底（forceDirectHeal）：** 80 级前随从尚未习得滋养，在 `< 60%` 重伤紧急分支中必须支持 `TryRegrowth(target, true)` 强制直疗模式，旁路 HoT 防顶限制吃直接治疗量保坦。


16. **物理近战背后找背与防同心圆死锁（Behind Kinematics & Anti-Carousel）：**
* 物理近战必须站位在目标正后方 1.5 码（`MoveFollow(victim, 1.5f, BEHIND_ANGLE)`），规避正面顺劈与招架加速。


* **同心圆死锁破解：** 必须检测怪物仇恨 `victim->GetVictim() == me`。若怪物看随从本人，禁止强行找背（会导致怪与随从同心圆旋转打转卡死），立即回落至 `MoveChase(victim, 1.5f)` 直线贴身交战；仅在怪物看主坦时才执行正后方绕背。




17. **远程近战盲区撤离迟滞区间（Hysteresis Zone）：**
* 猎人/法系进入近战盲区撤退时，若进退共用单一阈值（8 码），会导致“后退半步出 8 码立刻立定，下一帧被追上又后退”的高频抽搐死锁，长读条永久被打断。


* 必须建立迟滞区间：`< 8.0f` 触发撤退（`isRetreating = true`），且必须退至 `>= 15.0f` 安全线后才允许清空走位发生器立定施法。




18. **猎人远程自动射击通道维持（Auto Shot Activation）：**
* 原生引擎中 `me->Attack(victim, false)` 仅维持进战姿态，不驱动 Creature 的远程白字。


* 8~35 码内必须显式维持 `CURRENT_AUTOREPEAT_SPELL` 通道的 `AUTO_SHOT`（`checkGcd = false`），否则丢失 35% 白字伤害，且【蝰蛇守护】无法通过普攻吸取法力造成永久空蓝。




19. **随从武器毒药与命中模拟（Weapon Proc Simulation）：**
* Creature 随从无玩家物品栏系统与武器临时附魔（Weapon Poison Enchantments）。


* 依赖武器毒药的专精（刺杀贼、敏锐贼），必须在白字平砍与主力技能命中后，以节流方式（如 1000ms）向目标触发注入 `DEADLY_POISON`（维持 5 层）与 `INSTANT_POISON`，否则专精毒药伤害与联动机制永久缺失。




20. **近战终结技攒能挂起与产星通道锁死（Energy Pooling & Channel Lock）：**
* 连击点达到 4~5 星门槛但能量不足支付终结技（< 35 能量）时，必须 `return false` 原地挂起等待能量自然跳动，严禁贪打产星技导致星数溢出且把终结技推迟一整轮。


* 高级主力产星技习得后（如 40 级毁伤、20 级出血），能量不足时原地等待，**严格禁止降级施放低级技能**，杜绝低效填充偷跑终结技保留能量。




21. **自保免疫/假死/隐形脱困保护期与超时逃逸：**
* 寒冰屏障（冰箱）必须设立 **1500ms 最小保护期**、50% 安全血线与 2.5 秒超时兜底，防止开冰箱当帧因仇恨转移瞬间闪解自杀。


* 假死（Feign Death）必须包含仇恨解除检测与 **1500ms 硬性超时逃逸**，杜绝无坦或抵抗时倒地瘫痪 6 分钟被小怪围殴致死。


* 隐形术（Invisibility）必须强制跑满 **3000ms 淡入期** 后才现身，确保底层仇恨 100% 结算清零，防止刚隐身就假阳性现身再次秒 OT。




22. **长引导与前置光环防吞保护（Channeled & Proc Interlocking）：**
* 气定神闲（PoM）开启门禁必须显式排除【飞弹速射】（`!HasAura(AURA_MISSILE_BARRAGE)`），防止瞬发爆发光环被零耗蓝的高速引导飞弹起手吞噬。


* 瞬发奥冲等移动施法逻辑中，严禁在 `CanCast` 通过后无脑下发 `StopMoving()`，必须仅针对非瞬发读条才刹停立定。




23. **触发型被动天赋根源注入（Proc Passive Root Injection）：**
* 随从缺少真实天赋树，依赖暴击或特定打击触发的瞬发光环（如战争艺术 `59578`、血涌 `46916`、血之气息 `60503`、猝死 `52437`），必须在 `ApplyPassiveTalents` 中手工注入对应的**被动天赋根源 ID**（战争艺术 Rank 2 `53488`、血涌 Rank 3 `46915`、血之气息 Rank 3 `56638`、猝死 Rank 3 `29725`）。


* 仅检测触发光环而不注入被动根源，会导致随从终生无法触发特效，造成瞬发驱邪/瞬发猛击/压制/猝死斩杀通道永久坏死。




24. **圣盾术清仇恨主动解除与自律梯级（Divine Shield Auto-Cancel & Forbearance Tiering）：**
* 圣盾术（无敌）附带 -50% 伤害惩罚，且会造成 2 分钟【自律】（Forbearance）。


* 自保梯级必须遵循：**拯救之手（常规 OT 且血线 >= 20%） $\to$ 圣疗术（生命 < 15% 且无自律） $\to$ 圣盾术（生命 < 20% 且濒死压制/OT，且无自律）**。


* 严禁在 100% 满血 OT 时秒交圣盾术；圣盾术开启满 2000ms 且仇恨已交回坦克、血线安全（> 60% 或脱离近战）时，必须主动调用 `RemoveAurasDueToSpell(DIVINE_SHIELD)` 移除无敌，恢复全额输出。




25. **斩杀期怒气抽空饥饿反转（Execute Rage Starvation Inversion）：**
* 狂暴战【斩杀】会抽空所有怒气，在 20% 阶段若将斩杀排在首位，会导致随从怒气被瞬间榨干，主力 AP 技能【嗜血】与双持双段打击【旋风斩】因怒气不足永久饿死，总输出发生断崖式下跌。


* 狂暴战 FCFS 循环必须强制执行：**嗜血 $\to$ 旋风斩 $\to$ 血涌瞬发猛击 $\to$ 斩杀填充**，斩杀仅作为核心打击 CD 期间且怒气充盈时的泄怒补充。




26. **平砍队列控怒门禁与双持惩罚移除（Heroic Strike / Cleave Queue Simulation）：**
* 【英勇打击】与【顺劈斩】属于 on-next-swing 技能，不占 GCD，在 3.3.5a 挂起时可临时移除双持副手 27% 命中惩罚。
* 狂暴战设定 50 怒气门禁（`RAGE >= 500`），武器战设定 65 怒气门禁（`RAGE >= 650`），并配置 **1200ms 队列节流（heroicStrikeCooldown）**；斩杀期封锁英勇打击，杜绝平砍队列过度下发偷跑核心打击怒气。




27. **激怒急救同帧顺下（Enrage & Enraged Regeneration Co-Frame Dispatch）：**
* 【狂暴回复】底层硬性要求“激怒状态”且消耗 15 点怒气。


* 若随从无激怒，通过狂暴之怒/血性狂暴补齐前置后，**必须在当帧顺下施放狂暴回复**，绝对禁止 `return false` 把决策权让给后续 P3 打击循环，防止怒气被核心打击当帧抽空导致急救施法被拒。




28. **引导类斩杀通道等级自适应与断档主动破锁（Channel Execute Level-Adaptive & Breakout）：**
* 【吸取灵魂】、【精神鞭笞】等长引导通道单次耗时 3~15 秒，期间受全局通道守卫保护锁死决策流。若中途核心增伤 Debuff（鬼影缠身、吸血鬼之触）完全脱落或即将断档，随从会陷入长时间无增伤盲抽。


* 必须在 `UpdateAI` 通道守卫前进行破除检测：当引导技能为目标法术的合法 Rank（`channeledInfo->IsRankOf(...)`）、随从**已习得该天赋（`GetTalentRank(...) > 0`）** 且核心增伤 Debuff 断档、自管冷却就绪时，主动调用 `me->InterruptSpell(CURRENT_CHANNELED_SPELL)` 掐断通道重补增伤。


* 斩杀/引导开启门禁必须做天赋自适应处理（`!hasTalent || debuffRemaining >= threshold`），防止低等级随从因未习得天赋导致门禁恒假，终生无法开启引导通道。




29. **异常资源消耗通道底层直放（Non-Standard Power Bypass & Manual Gate）：**
* 【生命分流】（Life Tap）等以生命值（`POWER_HEALTH`）为消耗底座的法术，基类 `CanCast` 与 `ExecuteSpell` 的通用能量校验会读取非法内存偏移量，存在误判“能量不足”而把施法永久阻断的隐患。


* 必须彻底绕开 `CanCast`，直接调用底层 `me->CastSpell(me, spellId, false) == SPELL_CAST_OK` 直放，并由专精自管安全血线（> 50%）、蓝线（< 40%）及手工置位 `gcdTimer = 1500`。




30. **刷新类被动与首发施加解耦（DoT Refresh vs Initial Application）：**
* 【永恒痛苦】、【苦修与磨难】等机制只能“重置已存在 DoT 的持续时间”，绝不可能凭空施加 DoT。


* 随从斩杀期或转阶段转火新目标时，必须强制增加 `GetOwnDotRemaining(...) == 0` 首发判定，主动挂上基础 DoT，后续才交由通道技能自动无限续期，防止整段战斗核心 DoT 永久缺席。




31. **读条减免天赋与移动施法解耦（Cast-Time Reduction Proc & Dynamic Mobility）：**
* 类似【强化腐蚀术】使读条技能转为瞬发的天赋，必须在 `ApplyPassiveTalents` 注入根源被动（`17814`）。


* 通用施法函数在下发 `StopMoving()` 前必须动态判定是否已具备瞬发资质；瞬发法术完全放行移动中施放，仅对非瞬发读条在通过 `CanCast` 后主动刹停，杜绝跑位途中放弃施法的丢帧死锁。




32. **日月蚀双相记忆状态机（Eclipse Phase Memory State Machine）：**
* 3.3.5a 中【月蚀】（48518）与【日蚀】（48517）各自拥有 30 秒独立内置冷却（ICD），光环持续 15 秒。月蚀结束时，月蚀自身尚有 15 秒 ICD 残余；此时读条愤怒绝不可能触发月蚀，且愤怒无法触发日蚀。


* 必须引入双相记忆机制（`PHASE_SEEKING_LUNAR` 与 `PHASE_SEEKING_SOLAR`）：月蚀期间打星火并切换至 `SEEKING_SOLAR`，月蚀结束后在双蚀空窗期**坚决继续读条星火术**打入日蚀；日蚀期间打愤怒并切换至 `SEEKING_LUNAR`，日蚀结束后在空窗期读条愤怒打入月蚀。严禁在空窗期无脑切回愤怒导致日蚀永久报废。




33. **多阶被动天赋满级 Rank 注入契约（Max-Rank Passive Talent Injection Contract）：**
* 在 `ApplyPassiveTalents` 注入天赋树被动时，DBC 默认根源 ID 多为 Rank 1，其触发概率严重缩水（如日月蚀 Rank 1 `48516` 仅 33% 暴击触发率、自然之赐 Rank 1 `16880` 仅 33% 减读条几率、大地与月亮 Rank 1 `48506` 仅 4% 法术易伤）。


* 必须显式核对并注入 **Rank 3/5 满阶 Spell ID**（日月蚀 `48525` 100%、自然之赐 `61346` 100%、大地与月亮 `48511` 13%、强化虫群 `57848`、暗影交织 `15332`、悲惨 `33193`、黑暗 `15310`、暗影能量 `33225`、双手武器专精 `12714`、盗贼的尊严 `51701`、险恶召唤 `31220`），彻底规避随从被动触发率的隐形削弱。




34. **瞬发光环型法术 GCD 闭环与非通道判定（Aura Buff vs Channeled GCD Protocol）：**
* 【星辰坠落】（STARFALL）为持续 10 秒的瞬发独立光环，**绝非站桩引导通道**，严禁将其计入 `CURRENT_CHANNELED_SPELL` 守卫从而冻结随从走位与后续决策。


* 星辰坠落、【激活】（INNERVATE）、【横扫攻击】（SWEEPING_STRIKES）均占用 1.5 秒 GCD；施放成功后必须当帧交还决策权（`return true` 或 `return`），交由 `gcdTimer` 阻断后续施法，严禁同帧顺下与读条指令冲突导致动作丢帧与空转。




35. **锥形方向性法术朝向校正（Directional Cone Facing Guard）：**
* 【台风】（TYPHOON）等以自身为原点的正面锥形击退/减速法术，完全依赖施法者当前朝向结算判定范围。


* 随从在风筝撤离或贴坦避难时通常背对敌对目标，若直接施放台风会导致锥形落空；必须在施放判定通过后显式调用 `me->SetFacingToObject(victim)` 锁定朝向再执行施法。




36. **远程填充技能与全局射程收敛（Ranged Filler Range Alignment & Deadzone Prevention）：**
* 类似暗影牧师【精神鞭笞】基础 20 码（注入天赋暗影延伸后 24 码）的短射程填充技，专精的最大交战距离 `MAX_ENGAGE_DIST` 与理想站位 `IDEAL_SHOT_DIST` 必须严格向下收敛至填充技的作用上限（24.0f / 20.0f），绝不能盲目设置为 30 码。


* 若射程未对齐，在 20~24 码或 24~30 码区间随从挂完 DoT 后既不读填充技也不前压（因为判定为已在 30 码有效射程内），随从会陷入数秒原地发呆死锁。




37. **被动天赋根源 ID 与触发光环/目标 Debuff 严格解耦（Passive Root vs Proc Aura vs Debuff）：**
* 随从无天赋树，必须注入触发源天赋根源（如暗影交织 Rank 3 `15332`、悲惨 Rank 3 `33193`），严禁误注入 15s 临时增益光环（`15258` 会自然脱落断层）或直接向随从注入目标受到的易伤 Debuff（`33195` 会误挂在随从自身头上）。




38. **近战找背运动学与读条法术同帧守卫（Melee Behind Kinematics Casting Guard）：**
* 近战随从读条法术（如武器战猛击 0.5s、碎裂投掷 1.5s）在通过 `CanCast` 校验后 `StopMoving()` 刹停，但找背状态机 `MaintainMeleeBehindPositioning` 若缺少读条守卫，会在同帧检测到 `!isFollowing` 并立即重新下发 `MoveFollow`，造成读条 100% 同帧被走位秒断抽搐。


* 找背状态机顶部必须显式置位 `if (me->HasUnitState(UNIT_STATE_CASTING)) return;`，且读条打击施放后必须当帧 `return` 阻断后续平砍队列与多余指令。




39. **大招旋风通道与活体转火跟随（Bladestorm Channel & Dynamic Re-targeting）：**
* 【利刃风暴】（BLADESTORM）等高危自转光环期间严禁下发常规打击与读条，但必须允许 `MoveChase` 贴身跟随目标；


* 若目标中途死亡或脱战，必须当帧调用 `SelectAssistTarget()` 切换活体目标继续追击压制，杜绝 6 秒对着尸体空转。




40. **敌对目标增益法术目标类型契约（Hostile Buff Spell Targeting）：**
* 类似潜行者【预谋】（Premeditation `14183`），虽然在逻辑表现上是“为施法者赋予连击点增益”，但在 3.3.5a 底层 DBC 中硬性定义为敌对目标法术（`TARGET_UNIT_TARGET_ENEMY`，射程 30 码）。
* 调用 `CanCast` 与 `ExecuteSpell` 时目标必须传 `victim`，若传入友方目标 `me` 会被底层直接判定目标类型非法并 100% 拒绝施放。




41. **重置类技能（Preparation）门禁统计与执行口径严格对齐：**
* 【伺机待发】等长 CD 重置技能必须严格核对 3.3.5a 重置范围（消失、闪避、暗影步、预谋；暗影斗篷不在重置范围内）；


* 门禁判定函数中的冷却统计项（`CountActiveSurvivalCooldowns`）必须与施放成功后的清零列表完全一致，严禁“统计了却不清零”或“把不属于重置范围的技能计入门禁”，防止大招提前误放或空放。




42. **非玩家单位（Creature）连击点事件注入补偿（NPC Combo Point Proc Simulation）：**
* AzerothCore 底层处理连击点触发（如【盗贼的尊严】`EffectAddComboPoints`）时，原生包含 `m_caster->GetTypeId() == TYPEID_PLAYER` 硬编码校验。
* 随从作为 Creature 无法自动触发该机制，必须在专精中以被动天赋等级门禁配合内部节流（如 1000ms）手工调用 `AddComboPoints(victim, 1)`，真实还原核心被动回星手感。





---

## 4. 治疗专精通用「打地鼠雷达」标准实现 (GroupSnapshot)

所有治疗专精统一定义与复用以下结构及过滤算法：

```cpp
struct GroupSnapshot
{
    std::vector<Unit*> allies;          // 40 码内、视线可达、存活的友方单位
    Unit* lowestHpAlly{ nullptr };      // 全队最低生命百分比成员 (严格排除宠物)
    float lowestHpPct{ 100.0f };        // 团队最低血线指标
    Unit* mainTank{ nullptr };          // 主坦锚点
    float averageHpPct{ 100.0f };       // 全队平均生命百分比 (严格排除宠物)
};

void ConsiderAlly(GroupSnapshot& snap, Unit* unit)
{
    if (!unit || !unit->IsAlive() || !unit->IsInWorld()) return;
    if (unit->GetMap() != me->GetMap() || !unit->IsFriendlyTo(me)) return;
    if (unit->GetTypeId() == TYPEID_UNIT && unit->ToCreature()->IsTotem()) return;
    if (!me->IsWithinDist(unit, HEAL_RANGE) || !me->IsWithinLOSInMap(unit)) return;
    if (std::find(snap.allies.begin(), snap.allies.end(), unit) != snap.allies.end()) return;

    snap.allies.push_back(unit);

    if (!unit->ToPet())
    {
        float const hp = unit->GetHealthPct();
        if (hp < snap.lowestHpPct)
        {
            snap.lowestHpPct = hp;
            snap.lowestHpAlly = unit;
        }
    }
}

void FinalizeSnapshot(GroupSnapshot& snap)
{
    if (snap.allies.empty())
    {
        snap.lowestHpAlly = nullptr;
        snap.lowestHpPct = 100.0f;
        snap.averageHpPct = 100.0f;
        return;
    }

    if (!snap.lowestHpAlly)
    {
        for (Unit* ally : snap.allies)
        {
            if (ally && !ally->ToPet())
            {
                snap.lowestHpAlly = ally;
                snap.lowestHpPct = ally->GetHealthPct();
                break;
            }
        }
    }

    float total = 0.0f;
    uint32 validCount = 0;
    for (Unit* ally : snap.allies)
    {
        if (ally && !ally->ToPet())
        {
            total += ally->GetHealthPct();
            ++validCount;
        }
    }
    snap.averageHpPct = (validCount > 0) ? (total / static_cast<float>(validCount)) : 100.0f;
}

```

---

## 5. 当前专精开发进展与落地状态

项目目前已全面交付并封存 **19 个核心专精**（坦克集群 4 席、治疗集群 5 席、输出集群 10 席），全部实现逻辑闭环并达到工业级实战标准。

### 坦克集群（阶段一全部交付，具备副本实战品质）



* **防护战士 (`bot_protection_warrior`)：** 怒气自适应平滑注入、盾猛/复仇核心仇恨链、雷霆/冲击波群拉、盾牌格挡与破釜/盾墙梯级减伤、拦截破阵与断筋风筝。


* **防护圣骑士 (`bot_protection_paladin`)：** 969 技能循环、正义防御与神圣庇护多目标嘲讽路由、神圣之盾常驻维持、炽热防御者免死补偿。


* **鲜血死亡骑士 (`bot_blood_death_knight`)：** 符文/符能状态机模拟、冰冷触摸爆发仇恨、灵界打击动态已损回血、吸血鬼之血减伤。


* **野性熊坦 (`bot_bear_druid`)：** 怒气消耗模拟、重殴伪队列机制、横扫群拉与裂伤仇恨构建、狂暴回复与生存本能极限保命。



### 治疗集群（阶段一全五专精闭环交付并封存）



* **神圣圣骑士 (`bot_holy_paladin`)：** 单体道标折射、圣光术雕文溅射群抬、圣洁护盾维持、神圣震击跑位急救、秒拔神圣祈求减疗、自律解耦、防顺劈背身跟随与贴坦避难防抖。


* **戒律牧师 (`bot_discipline_priest`)：** 灵魂护体（0 CD 盾）、狂喜回蓝、争分夺秒（25% 急速）、主坦独立维持盾、单体急救盾与苦修首跳无缝衔接、绝望祷言自保、痛苦压制防猝死、魔法/疾病解耦驱散。


* **神圣牧师 (`bot_holy_priest`)：** 守护之魂 40% 受疗与免死急救、雕文 6 目标治疗之环智能群抬、圣光涌动 Proc 瞬发免费快疗、好运 2 层联动 1.8 秒大加、联结治疗双向回血叠层、希望圣歌长通道单体安全门禁、冥想精神回蓝补偿。


* **恢复萨满 (`bot_restoration_shaman`)：** 大地之盾充能与排己维护、水之护盾常驻回蓝、自然迅捷防误吞原子瞬发大加、激流与潮汐奔涌层数联动加速治疗波、雕文 4 目标治疗链智能跳跃（55% 濒死安全门禁）、四大图腾矩阵平滑展开、解耦三系驱散。


* **恢复德鲁伊 (`bot_restoration_druid`)：** 生命之树维持与瞬拔解控、回春前置防饥饿、三花滚动与濒危让位保护、树皮术减伤、迅捷治愈不吞 HoT 雕文、野性成长智能群抬、愈合强制直接治疗兜底、解诅咒/中毒精准收敛。



### 输出集群（阶段二大满贯：10 大核心输出专精闭环交付）

* **射击猎人 (`bot_marksmanship_hunter`)：**
* **远程运动学：** 8 码进入、15 码退出的近战盲区迟滞回正模型，杜绝高频抽搐走停，保障稳固射击站桩读条。


* **脱困与自保：** 假死脱困集成仇恨清空检测与 1.5 秒超时逃逸兜底，规避 6 分钟瘫痪死锁；危急交替威慑与逃脱。


* **平 A 与续航：** 激活 `CURRENT_AUTOREPEAT_SPELL` 远程自动射击通道；蝰蛇守护回蓝（<20% 切入，回升至 55% 切回龙鹰），依靠自动射击稳定吸蓝。


* **爆发与时序：** 准备就绪前置急速射击光环结束门禁，杜绝起手自吞爆发；毒蛇钉刺与奇美拉射击严格隔离自身 GUID，防止团队多猎人伤害失效。


* **仇恨转移：** 起手对主坦释放误导，支持玩家与随从坦克目标类型兼容。




* **刺杀潜行者 (`bot_assassination_rogue`)：**
* **近战找背运动学：** 区分怪物仇恨归属；怪盯防主坦时严格占住正后方 1.5 码规避招架加速与顺劈；怪看随从本人时自动回退为 `MoveChase` 直线近战贴身，彻底破解同心圆旋转打转死锁。


* **武器毒药模拟：** 随从普攻与毁伤命中自动模拟注入 5 层致命毒药与速效毒药，突破底层拦截，打通【毒伤 $\to$ 名天堑 $\to$ 切割自动续订 5 星】循环。


* **起手与连击循环：** 潜行起手窗口贴身（<=5 码）前挂起计时并封锁平砍挥砍，确保 100% 打出绞喉起手并触发灭绝 20 秒回能加速；4~5 星终结技攒能挂起（<35 能量等待回能）。


* **全等级平滑兼容：** 40 级习得毁伤后封锁降级邪恶攻击防止能量偷跑；40 级前邪恶攻击过渡产星、62 级前剔骨物理终结过渡。


* **瞬发 Off-GCD 顺下：** 冷血当帧顺下立即喂给毒伤必暴；暗影斗篷解除负面后当帧顺下继续输出。




* **奥术法师 (`bot_arcane_mage`)：**
* **自保解控安全门禁：** 寒冰屏障（冰箱）增加 1.5 秒最小保护期、50% 安全抬血线与 2.5 秒超时兜底，杜绝 50ms 闪解猝死；隐形术强制跑满 3 秒淡入期确保底层仇恨 100% 结算清零。


* **法力循环双相仲裁：** Conserve 平稳控蓝期（4 层奥冲 $\to$ 飞弹速射消层 $\to$ 奥术弹幕消层）；Burn 泄蓝期（奥术强化中或法力 >85% 时满层奥冲极限压榨伤害）。


* **飞弹速射与引导守卫：** 依靠 `CURRENT_CHANNELED_SPELL` 守卫奥术飞弹与唤醒引导通道；气定神闲开启门禁排除飞弹速射（防止被引导吞噬），且瞬发奥冲解除移动刹停，支持边跑边打满层瞬发奥冲。


* **续航体系与等级门禁：** 法力宝石降至 75% 蓝即吃以加速 2 分钟 CD 轮转，追加 28 级等级门禁防止低等级偷吃 3500 蓝；唤醒在 20~27 级自动绕过宝石 CD 门禁防死锁。


* **欺凌弱小常驻增伤：** 针对精英/首领自动维持瞬发【减速】（Slow）激活 12% 独立增伤；在奥术强化 15 秒核心泄蓝期屏蔽减速施放，防止偷跑宝贵的爆发 GCD。




* **惩戒圣骑士 (`bot_retribution_paladin`)：**
* **阵营圣印自适应与降阶：** 顺序轮询【腐蚀圣印】（部落）与【复仇圣印】（联盟），底层 `CanCast` 自动完成阵营种族判定；低等级或未习得时平滑降级【正义圣印】，彻底消除联盟惩戒骑无圣印裸奔隐患。


* **自律解耦自保梯级：** 拯救之手（常规 OT 且血线 >= 20% 优先减仇） $\to$ 圣疗术（生命 < 15% 且无自律时满血自救） $\to$ 圣盾术（生命 < 20% 且濒死压制/OT 时保命），根除满血 OT 秒交无敌浪费自律的严重缺陷。


* **圣盾术主动秒脱机制：** 进入无敌满 2000ms 且仇恨完全移交主坦、自身血线安全（> 60% 或脱离近战）时，主动剥离圣盾术光环，消除 -50% 伤害衰减惩罚。


* **FCFS 循环与被动根源注入：** 斩杀（愤怒之锤） $\to$ 审判（优先智慧审判回蓝，光明审判兜底） $\to$ 十字军打击 $\to$ 神圣风暴 $\to$ 战争艺术瞬发驱邪术（严格依赖战争艺术光环 `59578`，并在 `ApplyPassiveTalents` 注入天赋根源 `53488`） $\to$ 奉献（5 码内且法力 > 35%，动态适配奉献雕文 10s / 基础 8s 周期）。


* **运动学防同心圆死锁：** 严格执行 Rule 16，怪看主坦占正后方 1.5 码规避顺劈与招架加速，怪看随从切回直线近战贴身。




* **狂暴战士 (`bot_fury_warrior`)：**
* **姿态管理与平砍驱动：** 30 级及以上常驻【狂暴姿态】（2458），低等级平滑回退【战斗姿态】（2457）；UpdateAI 顶部双保险施法守卫，末帧调用 `DoMeleeAttackIfReady()` 驱动双持平砍与正义复仇/重伤流血。


* **FCFS 斩杀期防饿死逆转：** 嗜血（AP 加成核心 + 回血） $\to$ 旋风斩（双持双手双段 AoE，动态适配旋风斩雕文 8s / 基础 10s 冷却） $\to$ 血涌瞬发猛击（严格依赖光环 `46916`，并注入天赋根源 `46915`，绝对禁止近战读条） $\to$ 斩杀填充（仅作为核心打击 CD 期间的泄怒手段，杜绝抽空怒气饿死嗜血与旋风斩）。


* **平砍队列控怒（HS Queue）：** 严格设定 50 怒气门禁（`RAGE >= 500`）与 1200ms 队列节流，斩杀期封锁英勇打击；>=2 目标打顺劈斩，单体打英勇打击，稳定消除双持副手 27% 命中惩罚。


* **激怒与狂暴回复同帧顺下：** 生命 < 30% 触发自救；若缺失激怒前置，通过狂暴之怒/血性狂暴补齐后在当帧立即施放狂暴回复，绝不返回 false 让位给 P3 偷跑怒气，杜绝因不足 15 怒导致急救失败。


* **首领斩杀期爆发解封与突进：** 面对首领单位全程放行死亡之愿（2 分钟 CD）与鲁莽爆发（含斩杀期），普通精英仅在 > 10% 血量开启；8~25 码自动使用【拦截】（10 怒）突进贴身并昏迷目标。


* **核心被动光环补偿：** 注入重伤（12867，48% 武器流血）、暴怒（5% 物理全团暴击）、乱舞（30% 攻速）、泰坦之握（双持双手）及旋风斩/英勇打击雕文。




* **痛苦术士 (`bot_affliction_warlock`)：**
* **四重 DoT 滚动与弹道预读：** 鬼影缠身（8s CD，设定 2.5s 预读刷新窗口覆盖 1.5s 读条与 1s 弹道延迟，维持 20% 全域暗影增伤乘数） $\to$ 痛苦无常 $\to$ 腐蚀术 $\to$ 痛苦诅咒。


* **全等级自适应斩杀通道：** 目标 < 25% 切换【吸取灵魂】（死亡之拥 4 倍增伤）；引入等级自适应余量检测（`!hasTalent || remaining >= 3500ms`），杜绝低等级随从因未习得鬼影/无常导致斩杀门禁永久锁死。


* **引导死锁破除（Channel Breakout）：** 在 `UpdateAI` 施法守卫前主动检测，当随从引导吸取灵魂途中鬼影断档且 `hauntCooldown == 0`（限已习得鬼影等级），主动调用 `me->InterruptSpell` 掐断通道重补鬼影，杜绝十余秒无增伤盲抽。


* **首发腐蚀术与永恒痛苦联动：** 斩杀期先验 `GetOwnDotRemaining == 0`，转火与起手优先主动补挂腐蚀术，随后由鬼影与吸取灵魂的【永恒痛苦】被动自动刷新，彻底避免斩杀期腐蚀术永久缺席。


* **异常资源分流与血线自保：** 生命分流（LIFE_TAP）彻底绕过 `CanCast` 的 `POWER_HEALTH` 非法内存读取，直接调用底层 `CastSpell` 直放并置位 `gcdTimer = 1500`；严格限定生命 > 50% 且法力 < 40% 门禁，濒死交死亡缠绕回血自保，OT 交灵魂碎裂削减 50% 仇恨。


* **机动性与被动光环补偿：** 注入强化腐蚀术（17814）使腐蚀术瞬发，允许跑动风筝中直接上 DoT，仅非瞬发读条通过 `CanCast` 后才刹停；注入死亡之拥、暗影之拥、永恒痛苦、灭绝、传染及急速凋零/鬼影/生命分流雕文。




* **平衡德鲁伊 (`bot_balance_druid`)：**
* **日月蚀双相记忆状态机：** 月蚀（hasLunar）期间全力读条星火术并锁入 `PHASE_SEEKING_SOLAR` 相位，月蚀结束后在空窗期持续搓星火术诱发日蚀；日蚀（hasSolar）期间读条愤怒并锁入 `PHASE_SEEKING_LUNAR` 相位，日蚀结束后在空窗期搓愤怒诱发月蚀，彻底根除日蚀 0% 触发率死锁。


* **Rank 3 满阶被动根源注入：** 注入满级日月蚀（48525，100% 暴击触发双蚀）、自然之赐（61346，100% 暴击减读条 0.5s）、大地与月亮（48511，13% 团队法术易伤 + 3% 暴击）与强化虫群（57848，直伤联动加成），彻底消除 Rank 1 初级天赋带来的 67% 触发率缩水隐患。


* **星辰坠落与激活 GCD 闭环：** 识别星辰坠落（STARFALL）为非引导性独立光环（10s 独立群伤），动态适配星落雕文（60s / 90s CD）；激活（INNERVATE）与星落施放成功后当帧切断并交还决策流（`return true` / `return`），杜绝与读条技能同帧重叠下发导致的丢帧空转。


* **DoT 差异化调度与雕文协同：** 月火术完全断档首发必补，月蚀期间依赖【星火雕文】自动续期（剩余 > 3s 严禁手工补挂防止打断读条节奏）；虫群在日蚀期与平稳期积极维持，月蚀阶段让路星火读条密度。


* **台风朝向校正与远程站位：** 台风（TYPHOON）施放前强制调用 `me->SetFacingToObject(victim)` 锁定朝向，消除风筝避难时背身放空的浪费；瞬发法术跑动直接出手，读条法术 `CanCast` 通过后刹停立定，8/15 码迟滞模型稳定运转。




* **暗影牧师 (`bot_shadow_priest`)：**
* **24 码射程收敛与发呆死锁破除：** 注入【暗影延伸】Rank 2（17325）使精神鞭笞射程扩展至 24 码；将最大交战距离 `MAX_ENGAGE_DIST` 严格收敛至 24 码（理想站位 20 码），彻底根除 20~30 码挂完 DoT 后原地发呆数秒的死锁区。


* **DoT 滚动与鞭笞自动续期：** 吸血鬼之触（读条，维持全团回蓝） $\to$ 噬灵瘟疫（瞬发疾病） $\to$ 暗言术：痛（瞬发）；注入【苦修与磨难】Rank 3（47582）使精神鞭笞 100% 自动刷新痛，首发必挂痛，后续全程无损续订。


* **鞭笞引导通道与触断档自适应破锁：** 引导鞭笞途中，当且仅当随从已习得触（>=50 级）且触剩余 <= 1.5s（覆盖 1.5s 读条）时，主动调用 `me->InterruptSpell` 掐断鞭笞，当帧立即起手读条重补触，保障回蓝零真空。


* **形态锁定与脱战圣光增益破除：** 脱战检测到心火/坚韧/暗抗缺失时，瞬拔暗影形态打出圣光增益，增益齐备后再压回形态；战时绝对禁止脱形态，15% 减伤底座全额生效。


* **满阶被动根源注入：** 注入暗影交织 Rank 3 天赋根源（15332，100% 几率叠加 15258 光环至 5 层）、悲惨 Rank 3 天赋根源（33193，目标受法术命中+3%）、黑暗 Rank 5（15310）、暗影能量 Rank 5（33225），消除 ID 错乱与 Rank 1 数值缩水。


* **消散与暗影魔续航自保：** 生命 < 20% 或法力 < 10% 引导消散（90% 减伤回蓝，动态适配消散雕文 75s/120s CD）；精英/首领战蓝量 < 50% 施放暗影魔；生命 < 35% 瞬发套盾（虚弱灵魂门禁）。




* **武器战士 (`bot_arms_warrior`)：**
* **战斗姿态与双 Proc 触发链路：** 常驻【战斗姿态】（2457）；撕裂跳数 100% 触发【血之气息】（56638 Rank 3）激活高暴击【压制】（满阶 Rank 4 11585）；普攻命中 9% 触发【猝死】（29725 Rank 3）激活免费【斩杀】（保留 10 怒）。


* **读条守卫与同帧走位打断破除：** 在 `MaintainMeleeBehindPositioning` 顶部追加读条状态守卫 `if (me->HasUnitState(UNIT_STATE_CASTING)) return;`；猛击（0.5s 读条）与碎裂投掷（1.5s 读条）起手后当帧交还控制权 `return`，彻底终结读条被同帧下发的 `MoveFollow` 秒断抽搐的死锁。


* **碎裂投掷立定破盾与破甲：** 目标持有圣盾/保护/冰箱等免疫光环或首领缺少物理易伤时释放；施放前检测移动并显式刹停 `StopMoving()`，施放成功后当帧 `return true` 锁死决策流，确保 1.5s 读条完整落地，杜绝白烧 5 分钟 CD。


* **利刃风暴大招跟随与动态转火：** 6 秒自转期间锁定常规打击与读条，但放行 `MoveChase` 贴身跟随；原目标死亡中途自动通过 `SelectAssistTarget()` 动态重选活体目标继续追击，消除了对着尸体空转的真空期；动态适配风暴雕文（75s/90s CD）。


* **横扫攻击与 FCFS 循环：** >= 2 目标开启横扫攻击（占 GCD，成功后当帧切断交还控制权）；FCFS 循环严格执行：撕裂（维持气息） $\to$ 致死打击（30 怒门禁 + 创伤联动） $\to$ 压制（气息触发） $\to$ 斩杀（猝死或 <20%） $\to$ 猛击填充；65 怒气门禁在主循环后以 on-next-swing 队列驱动英勇打击/顺劈斩，斩杀期封锁泄怒。


* **主宰突进与激怒急救：** 注入【主宰】（64976）支持战时 8~25 码冲锋突进并提升 25% 暴击率；生命 < 30% 狂暴之怒/血性狂暴补齐激怒同帧顺下狂暴回复自救；生命 < 40% 且被 >=2 近战围攻开启反击风暴。




* **敏锐潜行者 (`bot_subtlety_rogue`)：**
* **暗影之舞与爆发时序压缩：** 8 秒影舞窗口（动态适配暗影之舞雕文 63420）；前置能量 $\ge 60$、近战 $\le 4$ 码且背身预检通过后开启；影舞期间预谋优先，星数 $\ge 4$ 立即打出刺骨消耗连击点，随后绝对优先读打【伏击】（满额享受敏锐大师 10% 与机遇 20% 增伤加成）。


* **伏击产星与预谋目标修正：** 伏击产星订正为满额 2 星；【预谋】（PREMEDITATION）施法目标校准为 30 码内敌对目标 `victim` 并稳定注入 2 星，根除对 `me` 施放导致的 100% 拒放阻断；起手预谋+暗影步闪现背身伏击秒接 4 星刺骨，爆发时序完全闭环。


* **战时暗影步突进贴身：** 引入 `TryCombatShadowstep`，目标处于 8~25 码区间时主动暗影步瞬移背身，提供 70% 移速与 20% 技能伤害加成，彻底终结转阶段与转火断攻慢跑。


* **盗贼的尊严模拟与能量挂起：** 绕过底层 `EffectAddComboPoints` 针对 Player 的限制，以 1000ms 内部节流稳定注入连击点，还原团队暴击白嫖星数手感；4~5 星终结技能量不足 35 原地挂起回能（铁律 20），杜绝贪打出血溢星与透支能量。


* **伺机待发与全自保重置对齐：** 濒死闪避、受控斗篷（Off-GCD 顺下）、极度濒危消失强行进潜清仇恨重置伏击起手；脱困后伺机待发（PREPARATION）重置项（消失/闪避/暗影步/预谋）与 `CountActiveSurvivalCooldowns` 统计口径严格一致，杜绝大招提前误放或漏重置。


* **背身找背与同心圆破锁：** 怪盯防主坦占正后方 1.5 码，怪盯防本人切换直线 `MoveChase` 贴身硬刚；出血无视朝向稳定产星易伤，白字平砍配合 1000ms 节流模拟 5 层致命毒药与速效毒药。





---

## 6. 下一阶段：系统级架构升级与沉浸感完善

至此，**全职业 19 个核心专精（坦克 4、治疗 5、输出 10）已达成大满贯闭环交付**。后续架构演进重点聚焦于战斗外围生态与机制增强：

1. **轻量级战斗护卫（Combat Guardian / 宠物伴随）体系：**
* **猎人：** 脱战检测并召唤座狼（Wolf），主人进战自动贴身撕咬平砍，被动光环注入狂怒之嚎 AP 增益；
* **痛苦术：** 脱战检测并召唤地狱猎犬（Felhunter），随从跟随攻击，数值保底暗影撕咬增益；
* **引擎铁律约束：** 宠物彻底剥离治疗打地鼠雷达（严禁治疗随从交大招加宠物），不自主索敌开怪，主人转火立刻同步转火，杜绝原生 Player Pet 的数据结构崩溃。




2. **顶层机制记忆库（SQLite）与归因分析器联调：**
* 激活团灭归因器（Post-Combat Attribution），针对首领灭团技建立致死 SpellID 记忆；


* 动态下发维度 A 压秒打断（80% 读条打断）与维度 B 危险坐标侧移规避。